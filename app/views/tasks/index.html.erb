<h3>This are all tasks</h3>

<% @tasks.each do |task|%>
  <div class="p-4 bg-white shadow-md rounded-lg max-w-md mx-auto">
    <!-- Comment Notification -->
    <div class="flex items-center justify-between py-2 border-b">
      <div>
        <h3 class="text-gray-900 font-medium"><%= task.name %></h3>
       <p class="text-sm text-gray-500">Temps: <%= task.time_required %> | Priorité: <%= task.priority %></p>
     </div>
      <input
        type="checkbox"
        class="w-5 h-5 text-blue-600 bg-gray-100 border-gray-300 rounded focus:ring-blue-500"
    />
    </div>
  </div>
<% end %>
<%= link_to "Finir la selection", chores_path, class: "btn" %>
